server:
  port: 18081

spring:
  application:
    name: xjc-core-service
  profiles:
    active: dev

eureka:
  instance:
    hostname: localhost
  client:
    service-url: #注册地址
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/